## Packages
date-fns | Used for date formatting and timer calculations
recharts | Used for the staff leave statistics chart

## Notes
- We assume dark mode is default for this premium professional aesthetic.
- Using `refetchInterval: 1000` on staff and leaves queries to ensure real-time synchronization across different agent PCs.
- Assuming the `@shared/routes` and `@shared/schema` export the exact structures defined in the prompt.
